Place post-event Sentinel-1 rasters here when you want to bypass the external RAW or GRD SAR toolchain.

Preferred filenames:

- `vv.tif`
- `vh.tif`
- `local_incidence_angle.tif` (optional)

Accepted alternatives also include `sigma0_vv.tif` and `sigma0_vh.tif`.
