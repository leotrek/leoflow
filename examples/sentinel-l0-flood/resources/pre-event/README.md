Place pre-event Sentinel-1 rasters here when you want flood detection to use backscatter change against a baseline scene.

Preferred filenames:

- `pre_event_vv.tif`
- `pre_event_vh.tif`

Accepted alternatives also include `pre_event_sigma0_vv.tif` and `pre_event_sigma0_vh.tif`.
